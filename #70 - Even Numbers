public List<Integer> evenNumbers(Integer n) {
    //code here
   /* if(n!=null && n>0 && n!=0){
        List<Integer> resultList = New List<Integer>();
        for(Integer i=2;i<=n*2;i=i+2){
            resultList.add(i);
        }
        return resultList;
    }else{
        return null;
    } */
    List<Integer> result = New List<Integer>();
        for(Integer i=2;i<=n*2;i+=2){
            result.add(i);
        }
        return result;
}
